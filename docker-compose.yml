name: ${CONTAINER_NAME}
services:
    app:
        image: mgazori/php-wordpress:${APP_IMAGE_VERSION:-latest}
          #image: wp_php:1
        container_name: ${CONTAINER_NAME}_app
        restart: unless-stopped
        tty: true
        user: "${UID}:${GID}"
        environment:
            SERVICE_NAME: ${CONTAINER_NAME}
            SERVICE_TAGS: ${APP_ENV}
            CONTAINER_ROLE: app
        working_dir: /var/www
        links:
            - mysql
        depends_on:
            - mysql
        volumes:
            - ./wordpress:/var/www
            # - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini:ro
        extra_hosts:
            - "host.docker.internal:host-gateway"
        networks:
            - app_network
        healthcheck:
            test: [ "CMD-SHELL", "php -r 'echo \"Health check\";'" ]
            interval: 30s
            timeout: 10s
            retries: 3
            start_period: 30s

    nginx:
        image: nginx:latest
        container_name: ${CONTAINER_NAME}_nginx
        restart: unless-stopped
        tty: true
        ports:
            - "${HTTP_PORT:-80}:80"
            - "${HTTPS_PORT:-443}:443"
        links:
            - app
        volumes:
            - ./wordpress:/var/www
            - ./docker/nginx/conf.d/:/etc/nginx/conf.d/
            - ./ssl:/etc/nginx/ssl
        networks:
            - app_network
        healthcheck:
            test: [ "CMD-SHELL", "curl -f http://localhost || exit 1" ]
            interval: 30s
            timeout: 10s
            retries: 3
            start_period: 30s

    mysql:
        image: mysql:8.0
        container_name: ${CONTAINER_NAME}_mysql
        restart: unless-stopped
        tty: true
        environment:
            MYSQL_DATABASE: ${DB_DATABASE}
            MYSQL_USER: ${DB_USERNAME}
            MYSQL_PASSWORD: ${DB_PASSWORD}
            MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}_root
            SERVICE_TAGS: ${APP_ENV}
            SERVICE_NAME: ${DB_DATABASE}
        volumes:
            - mysql_volume:/var/lib/mysql/
            - ./docker/mysql/my.cnf:/etc/mysql/my.cnf
        networks:
            - app_network
        healthcheck:
            test: [ "CMD", "mysqladmin", "ping", "-p${DB_PASSWORD}" ]
            retries: 3
            timeout: 5s

    redis:
        image: redis:latest
        container_name: ${CONTAINER_NAME}_redis
        restart: always
        networks:
            - app_network
        healthcheck:
            test: [ "CMD", "redis-cli", "ping" ]
            retries: 3
            timeout: 5s

    phpmyadmin:
        image: phpmyadmin
        container_name: ${CONTAINER_NAME}_phpmyadmin
        restart: always
        links:
            - mysql
        depends_on:
            - mysql
        ports:
            - "${PHPMYADMIN_PORT}:80"
        environment:
            - PMA_ARBITRARY=1
            - PMA_HOST=${CONTAINER_NAME}_mysql
            - PMA_PORT=3306
            - MYSQL_ROOT_PASSWORD=${DB_PASSWORD}_root
            - UPLOAD_LIMIT=600M
        volumes:
            - ./docker/phpmyadmin/config.user.inc.php:/etc/phpmyadmin/config.user.inc.php
        networks:
            - app_network

networks:
    app_network:
        driver: bridge
volumes:
    mysql_volume:
        driver: local
